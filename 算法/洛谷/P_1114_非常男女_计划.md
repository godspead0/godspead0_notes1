# P1114 “非常男女”计划

- 源文件：`洛谷/P_1114_非常男女_计划.cpp`
- 题目链接：https://www.luogu.com.cn/problem/P1114

```cpp
// P1114 “非常男女”计划 
// https://www.luogu.com.cn/problem/P1114

// #include<cstdio>
// #include<map>
// using namespace std;

// int main() {
//     int n, u, sum = 0, ans = 0;
//     scanf("%d", &n);
    
//     map<int, int> mp;  // 用来存储前缀和首次出现的位置
//     mp[0] = 0;  // 初始状态：前缀和为0时，位置为0
    
//     for (int i = 1; i <= n; i++) {
//         scanf("%d", &u);
        
//         // 将女生(0)视为 -1，男生(1)视为 +1
//         if (u == 0) u = -1;
        
//         sum += u;  // 更新前缀和
        
//         // 如果这个前缀和已经出现过，计算子区间的长度
//         if (mp.find(sum) != mp.end()) {
//             ans = max(ans, i - mp[sum]);
//         } else {
//             // 如果前缀和第一次出现，记录它的位置
//             mp[sum] = i;
//         }
//     }
    
//     printf("%d\n", ans);  // 输出最长的子区间长度
//     return 0;
// }
```
