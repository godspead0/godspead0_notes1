# P1563 [NOIP 2016 提高组] 玩具谜题

- 源文件：`洛谷/P_1563_NOIP_2016_提高组_玩具谜题.cpp`
- 题目链接：https://www.luogu.com.cn/problem/P1563

```cpp
// P1563 [NOIP 2016 提高组] 玩具谜题
// https://www.luogu.com.cn/problem/P1563


// #include<bits/stdc++.h>
// using namespace std;
// const int N = 1e6 + 10;
// struct Node {
//     int flag;  // 0: 朝内, 1: 朝外
//     string name;
// } q[N];
// int main() {
//     int n, m;
//     cin >> n >> m;
//     for (int i = 0; i < n; i++) {
//         cin >> q[i].flag >> q[i].name;
//     }
//     int now = 0;  // 当前小人的索引，从0开始
//     for (int i = 0; i < m; i++) {
//         int a, s;
//         cin >> a >> s;
//         if (a == 0) {  // 向左数
//             if (q[now].flag == 0) {
//                 now = (now - s + n) % n;  // 顺时针移动
//             } else {
//                 now = (now + s) % n;  // 逆时针移动
//             }
//         } else {  // 向右数
//             if (q[now].flag == 0) {
//                 now = (now + s) % n;  // 逆时针移动
//             } else {
//                 now = (now - s + n) % n;  // 顺时针移动
//             }
//         }
//     }
//     cout << q[now].name << endl;
//     return 0;
// }
```
