# P1042 [NOIP 2003 普及组] 乒乓球

- 源文件：`洛谷/P_1042_NOIP_2003_普及组_乒乓球.cpp`
- 题目链接：https://www.luogu.com.cn/problem/P1042

```cpp
// P1042 [NOIP 2003 普及组] 乒乓球
// https://www.luogu.com.cn/problem/P1042

//这题必须要保存数据，不知道为什么 我的代码其实能过 但是第一组样例不保存就是过不了

// #include <iostream>
// #include <string>
// using namespace std;
// // 处理比赛记录并输出结果
// void processMatch(const string& record, int winScore) {
//     int x = 0, y = 0; // 当前局的比分
//     for (char ch : record) {
//         if (ch == 'W') {
//             x++;
//         } else if (ch == 'L') {
//             y++;
//         }
//         // 当一方得分达到 winScore 且分差大于等于 2 时，结束当前局
//         if ((x >= winScore || y >= winScore) && abs(x - y) >= 2) {
//             cout << x << ":" << y << endl;
//             x = 0;
//             y = 0;
//         }
//     }
//     // 输出最后一局未完成的比分
//     if (x != 0 || y != 0) {
//         cout << x << ":" << y << endl;
//     }
// }

// int main() {
//     string input, record;
//     // 读取输入直到遇到 'E'
//     while (getline(cin, input)) {
//         for (char ch : input) {
//             if (ch == 'E') {
//                 break;
//             }
//             if (ch == 'W' || ch == 'L') {
//                 record += ch;
//             }
//         }
//         if (input.find('E') != string::npos) {
//             break;
//         }
//     }

//     // 处理 11 分制
//     processMatch(record, 11);
//     cout << endl; // 输出空行分隔
//     // 处理 21 分制
//     processMatch(record, 21);

//     // 如果输入中没有 'E'，输出 0:0
//     if (record.empty()) {
//         cout << "0:0\n\n0:0";
//     }

//     return 0;
// }
```
