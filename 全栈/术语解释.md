1.wiki：维基，指的是开源的知识库

2.开源：指代码完全开放，允许自由拷贝和修改

3.一个查询：指计算机题目中出现的查找过程

4.node：

5.cluster：集群，将**多个相同功能的服务器**组成一个逻辑整体，协同工作以提升系统性能、可用性和扩展性

6.DOM：Document Object Model文档对象模型，将网页转化为JS对象，这是前端操作界面的基础

![image-20251122115228558](C:\Users\agoni\AppData\Roaming\Typora\typora-user-images\image-20251122115228558.png)

7.VCS：版本控制，包括git和svn

8.CI：持续集成，开发者提交代码后，自动构建测试，保证代码质量

9.CD：持续部署，通过CI之后自动将代码部署到生产/测试环境

10.容器化：Containerization将应用机器依赖打包为容器，实现一次构建，到处运行

![image-20251122140740537](C:\Users\agoni\AppData\Roaming\Typora\typora-user-images\image-20251122140740537.png)

11.模块化：Module将代码拆分为独立可复用的模块

12.面向函数：FP以函数为核心

13.面向对象：OOP将代码进行封装，继承，多态为三大特性

14.面向切面：AOP

15.BOM：Browser Object Model浏览器对象模型，不依赖HTML文档，核心是windows对象

16.分布式：将系统拆分为多个独立模块，部署在不同服务器，通过网络协同工作（如微服务）

17.负载均衡：LB分发网络请求到集群中的多个节点，避免单节点过载

18.网关：Gateway系统入口，统一处理路由转发，鉴权，限流，跨域等功能

19.CDN：全球分布式节点，缓存静态资源（图片，JS，CSS）就近提供服务

20.IP：

21.协议：

22.事务：一组不可分割的操作

23.ACID：原子性（Atomicity）、一致性（Consistency）、隔离性（Isolation）、持久性（Durability）

24.泛型：Generic编写通用代码，提升代码复用性

25.接口：Interface不包含实现的类

26.缓存：Cache临时存储高频访问数据，减少数据库和服务器的查询压力

27.缓冲：buffer

28.正则：RegExp用于匹配，替换字符串的模式

29.并发：Concurrent多个任务在同一时间段交替执行

30.并行：Parallel多个任务在同一时刻执行

31.同步：

32.异步：

33.中间件：

34.CLI：命令行界面，与GUI图形化界面相比效率高

35.OS：操作系统Operating System

36.解析：parse将人类可以识别但是无固定结构的数据映射为程序可以直接进行操作的结构化模型

37.归档：

